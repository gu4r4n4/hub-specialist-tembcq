# Installation Note

The search functionality requires the `@react-native-picker/picker` package.

## To install:

```bash
npm install @react-native-picker/picker --legacy-peer-deps
```

Or if using yarn:
```bash
yarn add @react-native-picker/picker
```

After installation, restart the development server.
